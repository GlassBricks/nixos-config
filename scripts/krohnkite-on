#!/usr/bin/env bash
# Re-enable Krohnkite tiling (counterpart to stream-setup-min disabling it).
set -e
kwriteconfig6 --file kwinrc --group Plugins --key krohnkiteEnabled true
qdbus org.kde.KWin /KWin reconfigure
echo "krohnkite enabled"
